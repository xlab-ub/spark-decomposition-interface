# Vision / Object Detection (optional)

Off by default. Used only when `SPARK_ROBOT_BACKEND=go1` with camera hardware.

- `go1_camera.py` — camera stream and object detection for Unitree Go1

Requires OpenCV with GStreamer support and YOLO model weights. See `docs/SETUP_GO1.md`.
